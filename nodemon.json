{
  "watch": [
    "controllers",
    "models",
    "middleware",
    "config",
    "routes"
  ],
  "ext": "ts,json",
  "ignore": [
    "**/*.spec.ts",
    "node_modules",
    "dist"
  ],
  "exec": "npx ts-node --transpile-only index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}